Task: Expand hero image panel height
Summary: Changed the visible dashboard shell and React path hero from a fixed 300px cropped cover image to the source image aspect ratio with contain sizing, so the full hero image is visible and the briefing panel fits inside the hero.
Spec: docs/specs/briefing-panel-glass-overlay.md
Docs: N/A
TODO: N/A - user requested direct quick fix to main
Validation: npm test -- tests/dashboard.test.ts; npm run lint; npm run typecheck; DATABASE_URL=postgresql://postgres:postgres@localhost:5432/dutch_weather npm run build; headless browser check against local next start on port 3003
