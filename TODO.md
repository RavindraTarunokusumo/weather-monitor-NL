# TODO.md

This file contains active or future work only.

Completed sessions must be moved to `docs/iterations/archive/` and include the related spec path.

## Backlog

- [ ] Fix mobile current-weather overlay clipping — active spec: `docs/specs/briefing-panel-glass-overlay.md`
  - Anchor the top-right current-weather overlay inside the mobile hero image bounds.
  - Reduce the mobile overlay to an aligned weather icon + temperature chip.
  - Preserve tablet and desktop overlay sizing and full text content.

## Future Backlog

- [ ] Select exact first KNMI datasets for current observations and forecasts.
