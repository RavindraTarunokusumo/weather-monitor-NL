# TODO.md

This file contains active or future work only.

Completed sessions must be moved to `docs/iterations/archive/` and include the related spec path.

## Backlog

## Active Work

- [ ] 2026-05-02 root scaffold configuration baseline
  - Spec: `docs/specs/project-scaffold-local-dev.md`
  - [ ] Add root `pyproject.toml` with uv-managed dependencies and lint/test configuration.
  - [ ] Add `.pre-commit-config.yaml` and `.gitignore` for repository hygiene.
  - [ ] Generate `requirements.txt` and `uv.lock` from the resolved uv project metadata.

## Future Backlog

- [ ] Select exact first KNMI datasets for current observations and forecasts.
- [ ] Decide whether the first implementation milestone starts with a static prototype or backend skeleton.
