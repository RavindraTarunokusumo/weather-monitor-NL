# Spec-Driven Workflow Documentation - 2026-05-02

Completed session archived from `TODO.md`.

Spec: `docs/specs/spec-driven-workflow.md`

- [x] Update the agent workflow contract and related docs/templates so feature work is driven by accepted per-feature specs. Commit: `8cb939e`

Validation:

- `git diff --check` passed.
- `git diff --cached --check` passed before commit.
- Targeted `rg` workflow wording review completed.
- Manual Markdown link review completed for changed workflow docs.

Notes:

- Existing pre-spec archived sessions now include `Spec: N/A (completed before spec-driven workflow)`.
- Git note added to commit `8cb939e` using `.github/git_notes_template.md`.
